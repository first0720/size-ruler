# CLAUDE.md — 사이즈 자 (size-ruler)

이 파일은 Claude Code가 이 프로젝트에서 작업할 때 자동으로 읽는 지침 파일이다.
프로젝트 루트에 `CLAUDE.md`라는 이름으로 두면 세션 시작 시 자동으로 컨텍스트에 로드된다.

---

## 1. 로컬 연결 (Claude Code 설치·실행)

### 설치

```bash
# npm으로 설치 (Node.js 18+ 필요)
npm install -g @anthropic-ai/claude-code

# 또는 데스크톱 앱 사용
# https://claude.ai/download
```

### 실행

```bash
cd ~/projects/size-ruler   # 프로젝트 폴더로 이동
claude                     # 대화형 세션 시작
```

- 처음 실행하면 브라우저로 Anthropic 계정 로그인(OAuth)을 안내한다.
- `claude "질문"` — 일회성 질문 실행
- `claude -c` — 직전 세션 이어서 계속
- VS Code / JetBrains 확장으로 IDE 안에서도 사용 가능

### 로컬 환경 정보

- 셸: zsh
- 작업 디렉토리: 프로젝트 루트 기준으로 실행할 것
- 로컬 미리보기: `python3 -m http.server 8000` 후 http://localhost:8000

---

## 2. 필수 세팅

### 설정 파일 위치

| 파일 | 용도 |
|------|------|
| `~/.claude/CLAUDE.md` | 전역 지침 (모든 프로젝트 공통) |
| `<프로젝트>/CLAUDE.md` | 프로젝트별 지침 (이 파일) |
| `~/.claude/settings.json` | 전역 설정 (권한, 훅, 환경변수) |
| `<프로젝트>/.claude/settings.json` | 프로젝트 설정 (팀 공유, git 커밋) |
| `<프로젝트>/.claude/settings.local.json` | 개인 설정 (git 제외) |

### 권한(permissions) — `.claude/settings.json`

```json
{
  "permissions": {
    "allow": [
      "Bash(git status)",
      "Bash(git diff:*)",
      "Bash(git add:*)",
      "Bash(python3 -m http.server:*)"
    ],
    "deny": [
      "Bash(rm -rf:*)"
    ]
  }
}
```

### 배포 규칙 (중요)

- **배포는 `git push origin main`으로만 한다.**
- Netlify CLI, Vercel CLI 등으로 직접 배포하지 마.
- 배포·서버 구성은 기존 방식 그대로 유지하고 임의로 바꾸지 마.

---

## 3. 하네스(Harness) 기본 내용

하네스란 Claude가 실제로 동작하는 실행 환경(도구, 권한, 컨텍스트 관리)을 말한다.

### Claude가 쓰는 주요 도구

- **Read / Write / Edit** — 파일 읽기·생성·수정
- **Bash** — 셸 명령 실행 (권한 모드에 따라 승인 필요)
- **Glob / Grep** — 파일·코드 검색
- **Agent(서브에이전트)** — 큰 탐색·병렬 작업을 별도 에이전트에 위임
- **WebSearch / WebFetch** — 웹 검색·페이지 가져오기
- **Skill(슬래시 명령)** — `/init`, `/code-review`, `/simplify` 등

### 컨텍스트 로딩 순서

1. `~/.claude/CLAUDE.md` (전역 지침)
2. 프로젝트 루트의 `CLAUDE.md` (이 파일)
3. 메모리 인덱스 (`~/.claude/projects/.../memory/MEMORY.md`)

대화가 길어지면 자동으로 요약(compact)되어 다음 컨텍스트로 이어진다.

### 권한 모드

- 기본: 파일 수정·명령 실행 전 사용자 승인 요청
- `/permissions` 로 허용 목록 관리 (터미널 세션에서)
- 자주 쓰는 안전한 명령은 settings.json의 allow에 등록해 프롬프트 줄이기

### 훅(hooks) — 자동화

특정 이벤트(도구 실행 전/후, 세션 종료 등)에 셸 명령을 자동 실행하려면
settings.json에 hooks를 등록한다. "매번 X 할 때마다 Y 해줘" 같은 자동화는
기억이 아니라 훅으로만 보장된다.

---

## 4. 작업 원칙 (요약)

우선순위: **정확성 > 검증 > 최소 변경 > 명확성 > 유지보수성**

- 파일·API·스키마가 존재한다고 가정하지 말고 먼저 읽어서 확인해.
- 수정 전에 관련 파일을 읽고, 수정 후에는 테스트·실행으로 검증해.
- 요청된 작업에만 변경을 국한하고, 관련 없는 리팩토링은 하지 마.
- 가장 단순한 해결책을 선호하고, 불필요한 의존성·추상화를 추가하지 마.
- 기존 프로젝트의 관례와 스타일을 따라.
- 막히면 멈추고 무엇이 막혔는지, 무엇이 검증됐는지 명확히 보고해.
- 검증 없이 "성공했다"고 주장하지 마.

---

## 5. 프로젝트별 정보

- **프로젝트 이름:** 사이즈 자 (size-ruler)
- **한 줄 정의:** 발 길이(mm)를 기준으로 국가별 신발 사이즈와 브랜드별 권장 사이즈를 알려주는 정적 사이트
- **기술 스택:** 순수 HTML + CSS + 바닐라 JS. 빌드 도구·프레임워크 없음
- **개발 서버 실행:** `python3 -m http.server 8000`
- **빌드:** 없음 (정적 파일 그대로 배포)
- **테스트:** 없음. 브라우저에서 육안 확인 + Lighthouse
- **배포:** `git push origin main` → Cloudflare Pages 자동 배포
- **수익 모델:** 구글 애드센스

### 이 프로젝트의 절대 규칙

이 사이트는 **검색 유입이 전부**다. 아래 규칙은 기능보다 우선한다.

1. **모든 콘텐츠는 정적 HTML에 있어야 한다.**
   환산표, 브랜드 설명, FAQ 본문을 JS로 생성하지 마. JS는 이미 HTML에 있는 값을
   강조·갱신하는 용도로만 쓴다. JS가 꺼져도 페이지의 정보 가치가 100% 유지돼야 한다.

2. **React·Next.js·Vue를 도입하지 마.**
   이 사이트의 요구사항으로는 과하고, 초기 로딩만 느려진다.
   "더 깔끔하게 만들어줄까요" 식의 프레임워크 제안 금지.

3. **페이지 하나당 키워드 하나.**
   새 주제(예: 의류 사이즈, 반지 호수)를 추가할 때는 기존 페이지에 섹션을 붙이지 말고
   `/clothing/`, `/ring/` 처럼 **별도 경로의 별도 HTML 파일**로 만든다.

4. **데이터는 확인하고 넣는다.**
   사이즈 환산값이나 착용 경향을 추측으로 채우지 마. 근거가 없으면 항목을 아예 빼고
   나에게 물어봐. 이 사이트는 정확성이 유일한 차별점이다.

5. **핏 데이터는 브랜드가 아니라 모델 단위로만 쓴다.** (검증으로 확인된 규칙)
   "나이키는 반업" 같은 브랜드 단위 일반화는 실제로 틀린다. 같은 브랜드 안에서도
   모델별로 반업과 반다운이 갈린다. 실제 변수는 **모델 × 발볼** 두 개다.
   - 모든 핏 항목에는 모델명을 명시할 것 (예: "아디다스 가젤", "아디다스"가 아니라)
   - 발볼 조건을 함께 적을 것 (칼발 / 보통 / 넓음)
   - `.src` 클래스로 **출처를 항상 표기**할 것. 출처 없는 항목은 게시하지 마
   - 브랜드 공식 안내 > 리셀 플랫폼 사이즈 팁 > 커뮤니티 다수 의견 순으로 신뢰
   - 뉴발란스는 mm보다 발볼 코드(B/D/2E/4E)가 더 중요하다. 이 점을 빠뜨리지 마

6. **외부 스크립트는 최소한으로.**
   애널리틱스·애드센스 외의 서드파티 스크립트를 추가하지 마.

7. **접근성 기준을 지킨다.**
   키보드 포커스 표시, 색 대비 4.5:1 이상, `prefers-reduced-motion` 존중.

### 페이지 추가 체크리스트

새 HTML 페이지를 만들 때 아래를 빠뜨리지 마.

- [ ] `<title>` — 60자 이내, 주요 키워드가 앞쪽에
- [ ] `<meta name="description">` — 155자 이내
- [ ] `<link rel="canonical">` — 실제 도메인으로
- [ ] FAQ가 있으면 `FAQPage` JSON-LD
- [ ] `sitemap.xml`에 URL 추가
- [ ] 관련 페이지끼리 본문 내 링크 연결

### 주의사항

- 브랜드별 권장 사이즈는 **일반적 경향**이지 공식 수치가 아니다. 단정적 표현을 쓰지 말고
  푸터의 면책 문구를 지우지 마.
- 애드센스 코드를 넣을 때 콘텐츠 사이에 광고를 과하게 끼워 넣지 마. 정책 위반 위험이 있고
  사용자 경험도 나빠진다. 상단 1개, 표 아래 1개면 충분하다.
