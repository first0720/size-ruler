# 프로젝트 인수인계 — 사이즈 자 (size-ruler)

> 새 대화창에서 작업을 이어갈 때 이 파일을 통째로 붙여넣으면 됩니다.
> 마지막 갱신: 2026-09-25 — 신발 모델 50개 + 의류·아동·반지·발볼 가이드, 배포·색인 대기 중

---

## 1. 이 프로젝트가 뭔가

**애드센스 수익형 정적 웹사이트.** 발 길이(mm)와 신체 치수 기준으로
신발·의류 사이즈를 환산해주는 "틈새 정보 DB" 사이트.

유튜브 영상(바이브코딩으로 월 10만원 버는 사이트)을 보고 시작했다.
그 방식의 핵심은 **간단한 도구/정보 사이트 → 검색 유입 → 애드센스**다.

### 왜 이 주제를 골랐나

여러 후보(계산기, 변환기, 지역정보, 지원금, 요금비교) 중 **규격·호환 DB**를 선택.
그중에서도 신발 사이즈를 고른 이유:

- 데이터가 **영원히 안 변한다** — 갱신 부담 제로, API 불필요
- 검색이 **사시사철** 나온다 (시즌 안 탐)
- 기존 환산기들은 표만 있고 **"그래서 나이키는 몇 신어야 하냐"에 답을 안 한다**
  → 이 빈틈이 유일한 차별점

### 수익 구조

- 목표: 월 10만원 = 애드센스 기준 **월 2~3만 방문**
- 새 도메인은 구글 노출까지 **3~6개월**
- 첫 두 달은 아무 일도 안 일어나는 게 정상. 여기서 포기하면 끝.

---

## 2. 지금까지 한 일

| 단계 | 상태 |
|------|------|
| 주제 선정 | 완료 — 신발/의류 사이즈 환산 |
| 메인 페이지 제작 | 완료 |
| 브랜드 데이터 검증 | 완료 — 초기 데이터 절반이 틀려서 전면 수정 |
| 모델별 페이지 9개 | 완료 |
| 의류 카테고리 2개 | 완료 |
| 배포 준비 파일 | 완료 (스크립트·404·파비콘·헤더) |
| 품질 다듬기 | 완료 — 대비/메타/입력기능/표강조 (아래 8-1 참고) |
| 도메인 구매 | 완료 — **sizeruler.com** |
| 도메인 교체 | 완료 — 전 페이지 canonical 반영 검증됨 |
| GitHub + Cloudflare Pages 배포 | 완료 — sizeruler.com, www 리다이렉트 |
| 서치콘솔·서치어드바이저 등록 | 완료 (2026-09-25) — 색인 대기 중 |
| ads.txt · 네이버 소유확인 태그 복구 · IndexNow | 완료 (2026-09-25) — 태그는 09-02 홈 수정 때 지워졌던 것을 되살림 |
| 모델 표 US·UK·EU를 브랜드 공식표로 교체 | 완료 (2026-09-25) — 나이키·아디다스·뉴발란스·반스·컨버스 |
| **애드센스 신청** | **미완 — 색인 확인 후 (여기서 멈춰 있음). 광고 코드는 전 페이지에 들어가 있음(404 제외)** |

---

## 3. 가장 중요한 교훈 (반드시 유지할 것)

### 브랜드 단위 데이터는 반드시 틀린다

처음에 "나이키 +5mm, 아디다스 +5mm, 컨버스 -5mm" 식으로 브랜드마다 보정값을
넣었는데, 검증해보니 **절반이 틀렸다.**

- **나이키 +5 → 틀림.** 에어포스1 로우는 정사이즈가 기준
- **컨버스 -5 → 정반대.** 척 70은 오히려 반 사이즈 **업**이 권장
- **아디다스 +5 → 조건부.** 삼바는 정사이즈, 가젤은 반업. 같은 브랜드인데 다름

**실제 변수는 `모델 × 발볼` 두 개다.** 브랜드로 뭉뚱그리면 반드시 틀린다.

### 그래서 만든 규칙

1. 핏 데이터는 **모델 단위**로만 쓴다 ("아디다스"가 아니라 "아디다스 가젤")
2. **발볼 조건**(칼발/보통/넓음)을 항상 함께 적는다
3. `.src` 클래스로 **출처를 반드시 표기**한다. 출처 없으면 게시하지 않는다
4. 신뢰도 순서: **브랜드 공식 > 리셀 플랫폼(KREAM) > 커뮤니티**
5. 근거를 못 찾으면 **항목을 아예 뺀다** (아식스가 이 이유로 제외됨)

### 환산표도 브랜드마다 다르다 (2026-09-25 확인)

US·UK·EU 숫자는 브랜드가 정한다. 공용 환산표 하나로 전 브랜드를 표시하면 틀린다.

- 270mm: 나이키·아디다스·뉴발란스·반스 US 9, **컨버스 US 8.5** (컨버스 US 남 9 = 27.5cm)
- 240mm 아래에서는 나이키 US가 다른 브랜드보다 반~한 사이즈 작다 (230mm: 나이키 4, 아디다스·뉴발란스 5)
- EU 38: 아디다스·뉴발란스 235mm, 나이키·반스 240mm, 컨버스 245mm
- 여성 전용 상품은 여성 사이즈표를 따로 쓴다 (뉴발란스 여성 EU 38 = 245mm, 남녀공용은 235mm)
- 공식표가 KR 표기와 발 길이를 연결하는 방식도 다르다: 나이키 270 = 발 26.2cm, 아디다스 270 = 26.3cm,
  뉴발란스·컨버스 270 = 27cm, 반스 270 = 26.9cm (아래 7번 '결정 필요' 참고)

### 콘텐츠는 정적 HTML에 있어야 한다

검색 유입이 전부인 사이트다. 표·설명·FAQ를 JS로 렌더링하면 안 된다.
JS는 이미 HTML에 있는 값을 갱신하는 용도로만 쓴다.
(초기 버전이 이 실수를 했다가 수정함)

---

## 4. 현재 사이트 구조

모델 목록의 기준은 `build_models.py`의 `MODELS` 리스트다 (신발 모델 50개).
여기에 페이지를 일일이 적지 않는다. 적어두면 금방 틀어진다.

```
/                        전체 환산표 + 인터랙티브 줄자 + 모델 목록
/<모델 슬러그>/          신발 모델 페이지 (build_models.py)
/clothing-size-men/      남성 — 숫자가 곧 가슴둘레 cm (build_clothing.py)
/clothing-size-women/    여성 — 44·55·66은 표준이 없음을 설명
/kids-shoe-size/  /kids-clothing-size/  /ring-size/   손으로 만든 페이지
/newbalance-width/       뉴발란스 발볼 가이드 (build_guides.py)
/us-size-chart/          미국 신발 사이즈표 — 5개 브랜드 공식표 비교 (build_guides.py)
/eu-size-chart/          유럽 신발 사이즈표 — EU↔mm, 여성 전용표 포함 (build_guides.py)
/about/                  사이트 소개 — 출처 신뢰 순서와 검증 원칙 (build_guides.py)
/privacy/                개인정보처리방침

공통 파일
404.html                 없는 주소 안내 (noindex)
style.css                공용 스타일
favicon.svg              줄자 눈금 모티프
_headers                 Cloudflare 캐시 설정
robots.txt / sitemap.xml 생성기가 자동 갱신
ads.txt                  애드센스 판매자 확인 (pub-8018650083353602)
<32자리 키>.txt           IndexNow 키 파일. 지우면 indexnow.py가 실패한다
```

### 생성기와 도구

| 파일 | 역할 |
|------|------|
| `build_models.py` | 신발 모델 페이지 — `MODELS` 리스트 편집 후 실행 |
| `build_clothing.py` | 의류 페이지 — `PAGES` 리스트 편집 후 실행 |
| `build_sitemap.py` | 폴더 스캔해서 sitemap.xml + robots.txt 생성 |
| `build_og.py` | og:image(1200x630) 생성. 문구 바꾸면 재실행 |
| `build_guides.py` | 개념 가이드 페이지 (발볼, 미국·유럽 사이즈표, 사이트 소개). 비교표 숫자는 `build_models.py`의 공식표를 import |
| `indexnow.py` | 사이트맵 URL 전체를 네이버·빙(IndexNow)에 알림. 배포 1~2분 뒤 실행 |
| `add_models.py` | `batch_*.py`(첫 줄 `# SIZERULER_BATCH`)의 모델·홈 카드를 넣고 생성기 실행 |
| `cleanup.py` | 404.html을 `MODELS` 기준으로 재생성 + 푸터 링크 확인 (여러 번 돌려도 안전) |

**모델 페이지는 직접 수정하면 안 된다.** 다음 실행 때 덮어써진다.
내용을 고치려면 생성기의 리스트를 고친다.

### 페이지 추가 절차

```bash
# 1. batch_<이름>.py 작성 — MODELS_BLOCK(모델 dict)과 CARDS_BLOCK(홈 카드). sources 필수!
# 2. 모델 추가 + 홈 카드 + 페이지·사이트맵 생성 (batch 파일은 자동 삭제)
python add_models.py
# 3. 404.html에 새 모델 반영
python cleanup.py
# 4. 배포
git add -A && git commit -m "모델 추가" && git push origin main
# 5. 배포가 반영된 뒤(1~2분) 네이버·빙에 알림
python indexnow.py
```

### 브랜드 공식 환산표

`build_models.py`의 `BRAND_CHARTS`가 브랜드 이름(모델명 첫 단어)으로 공식표를 고른다. 나이키·아디다스·뉴발란스·반스는
브랜드 전체, 컨버스는 `"converse_chart": True`인 모델(척테일러·척 70)만. 나머지 브랜드는 일반 환산표 `MAP`을 쓰고
표 아래에 '일반 환산표 기준'이라고 적힌다. 새 브랜드의 공식표를 확인하면 `*_MAP`을 추가하고 `BRAND_CHARTS`에 등록한다.

---

## 5. 페이지 구조 (모든 모델 페이지 공통)

```
결론 (노란 박스)  ← 검색해서 들어온 사람이 스크롤 없이 보는 답
    ↓
발 길이별 권장 사이즈 표
    ↓
발볼별 가이드 (칼발/보통/넓음)
    ↓
알아둘 점 (3문단)  ← 체류시간이 여기서 나옴. 다른 사이트에 없는 부분
    ↓
FAQ (JSON-LD FAQPage 포함)
    ↓
다른 모델 링크 6개 (같은 브랜드 최대 3개 + 다른 브랜드. 자기 다음 모델부터 돌아가며 골라 모든 모델이 링크를 받음)
```

### 디자인 방향

- 줄자/측정 도구 컨셉. 노란색(#FFCE00)은 실제 줄자 색에서 가져옴
- 폰트: Oswald(숫자·제목) + Pretendard(본문) + IBM Plex Mono(데이터)
  - Pretendard는 **가변 동적 서브셋**을 쓴다 (`pretendardvariable-dynamic-subset`, font-family `"Pretendard Variable"`).
    정적 전체 버전은 페이지마다 2~3MB라 바꿨다 (동적 서브셋은 350~480KB)
- 메인의 인터랙티브 줄자가 시그니처 요소
- 프레임워크 없음. 순수 HTML/CSS/바닐라 JS

---

## 6. 기술 스택 / 배포

| 항목 | 값 |
|------|-----|
| 스택 | 순수 HTML + CSS + 바닐라 JS (빌드 도구 없음) |
| 로컬 확인 | `python3 -m http.server 8000` |
| 배포 | Cloudflare Pages, `git push origin main`이 전부 |
| 빌드 설정 | **Framework preset: None / Build command: 비움 / Output: `/`** |
| 수익화 | 구글 애드센스 (미신청) |

### style.css를 고칠 때 (중요)

`_headers`가 style.css를 7일 캐시하고 Cloudflare 엣지도 캐시한다. 주소가 같으면 방문자는 최대 7일간 예전 CSS를 받는다.
그래서 모든 페이지가 `/style.css?v=2`처럼 버전을 붙여 부른다. **CSS를 고치면 숫자를 올린다:**

```bash
grep -rl 'style.css?v=2' --include=*.py --include=*.html . | xargs sed -i 's#style.css?v=2#style.css?v=3#g'
```

(2026-09-25에 폰트 이름을 바꾸면서 이걸 안 해서, 한 시간가량 본문이 기본 글꼴로 보였다.)

### 도입하면 안 되는 것

React, Next.js, Vue — 이 사이트 요구사항에 과하고 초기 로딩만 느려진다.
Netlify CLI, Vercel CLI로 직접 배포하지 말 것.

---

## 7. 다음에 할 일 (순서대로)

### 즉시

1. ~~도메인 구매~~ — **sizeruler.com** 완료

2. ~~도메인 교체~~ — `./setup-domain.sh sizeruler.com` 완료.
   canonical 12개, og:url 12개, sitemap 12줄, robots, 생성기까지 전부 반영.
   생성기를 다시 돌려도 되돌아가지 않는 것까지 확인함.

3. **GitHub push → Cloudflare Pages 연결 → 커스텀 도메인 + www 등록**

4. **구글 서치콘솔 + 네이버 서치어드바이저 등록, 사이트맵 제출**
   - 배포만 하고 이걸 안 하면 몇 달이 지나도 검색에 안 잡힌다

5. **색인 확인(1~2주) 후 애드센스 신청**
   - 광고는 상단 1개, 표 아래 1개면 충분. 과하면 정책 위반

상세 절차는 `DEPLOY.md`에 9단계로 정리돼 있음.

### 결정 필요 (2026-09-25)

1. **사이즈 기준: '발 길이' vs '평소 사이즈'.** 홈은 "발 길이를 mm로 재서 고르라"고 하고 모델 표 첫 열도 '내 발 길이'다.
   그런데 나이키·아디다스 공식표는 발 길이보다 7~8mm 큰 표기를 권한다 (발 26.2cm → 270). 뉴발란스·반스·컨버스는 거의 같다.
   정사이즈·반업 같은 핏 정보(KREAM 등)는 원래 '평소 신는 사이즈' 기준이다.
   추천: 표·입력칸을 '평소 사이즈'로 바꾸고, 발 길이만 아는 사람은 '발 사이즈 재는 법' 페이지에서 공식표로 평소 사이즈를 찾게 한다.
   결정 전까지 공식표의 발 길이 열은 페이지에 싣지 않았다 (사이트 안에서 모순이 드러나므로).
2. **사이트 소개 연락처** — contact@sizeruler.com(Cloudflare 이메일 라우팅) 추천. 정해지면 /about/에 추가

### 공식표를 아직 대조하지 않은 브랜드

아식스·오니츠카 타이거·호카·살로몬·온·푸마·리복·버켄스탁·닥터마틴·어그·크록스·팀버랜드, 컨버스 잭퍼셀.
이 브랜드 모델 표는 일반 환산표이고 표 아래에 그렇게 적혀 있다. 아식스 공식 차트 주소는 바뀌어 있었다(2026-09 기준 404).

### 데이터 보강 (약한 곳)

- ~~반스 올드스쿨~~ — 반스 공식 사이즈 차트(US 남 9 = 발 길이 27cm)·와이드 폭 안내로 보강함 (2026-09-25).
  꽉끈 반업·컴피쿠시 1업은 여전히 커뮤니티 근거
- ~~컨버스 척테일러 클래식~~ — 컨버스 공식 "크게 나옴, US 반 다운" 반영 (2026-09-25)
- ~~컨버스 US 표기~~ — 컨버스 US는 다른 브랜드보다 반 칸 크다 (US 남 9 = 27.5cm, 국내 270mm = US 8.5).
  모델에 `"converse_chart": True`를 주면 `CONVERSE_MAP`(공식 차트)으로 US/UK/EU 열을 만든다 (2026-09-25)
- ~~컨버스 척 70~~ — 공식 근거 우선으로 "발 길이 그대로, 발볼 넓으면 반업"으로 변경. KREAM 반업 의견은 보조로 표기 (2026-09-25)
- **잭퍼셀** — 컨버스지만 공식 차트가 척테일러·척 70만 명시해 `converse_chart` 미적용. 확인 필요

### 확장 후보

- 신발 모델: 근거 확보가 선행 조건. 나이키 코리아 상품 페이지에는 핏 안내가 없는 경우가 많아
  RunRepeat 실측·착용자 투표가 쓸 만한 대안 (보메로 5가 이 방식)
- 새 카테고리: 모자, 브라

**단, 지금은 페이지를 늘리는 것보다 배포해서 색인 시계를 돌리는 게 우선이다.**
콘텐츠는 색인 기다리는 동안 계속 추가할 수 있지만, 배포는 미룰수록 손해다.

---

## 8. 검증된 모델 데이터 요약

| 모델 | 결론 | 주요 근거 |
|------|------|-----------|
| 나이키 에어포스1 로우 | 정사이즈 (칼발 반다운, 발등 높으면 반업) / 미드·하이 반업 | 파페치, KREAM, 나이키 코리아 |
| 나이키 덩크 로우 | 정사이즈 (SB는 반업) | 나이키 코리아 공식 가이드 |
| 아디다스 삼바 | 보통 발볼 정사이즈, 넓으면 반업 | 아디다스 코리아 공식 |
| 아디다스 가젤 | 반업 (발볼 넓으면 1업) | KREAM |
| 컨버스 척 70 | 발 길이 그대로, 발볼 넓으면 반업, 하이 1업 의견 | 컨버스 공식 차트, KREAM |
| 컨버스 척테일러 | 칼발 정사이즈, 발볼 있으면 반~1업 | 컨버스 공식 차트, 후기, 커뮤니티 |
| 뉴발란스 530 | 정사이즈 | KREAM |
| 뉴발란스 993 | D 기준 정사이즈. **mm보다 발볼 코드(B/D/2E/4E)가 먼저** | 뉴발란스 발볼 옵션 |
| 반스 올드스쿨 | 정사이즈, 꽉끈이면 반업, 컴피쿠시는 1업 | 반스 공식 차트·와이드 안내, 커뮤니티 |

### 의류 핵심

- **남성복 숫자 = 신체 치수.** 상의 95 = 가슴둘레 95cm / 바지 32인치 = 허리 81cm
- **여성복 44·55·66 = 표준 없음.** 브랜드별 관습이라 정확한 환산 불가능
  → 이걸 솔직하게 설명하는 게 이 페이지의 차별점.
  다른 사이트들은 없는 표를 있는 척 인쇄해놓는다

---

## 8-1. 다듬기 작업에서 고친 것 (참고)

브라우저(Playwright)로 실제 검증까지 마친 항목들.

| 문제 | 조치 |
|------|------|
| 색 대비 2건이 WCAG AA 미달 (4.38:1, 3.39:1) | `--muted` #71716A→#5F5F58, `.src` #8C8C84→#6B6B63 |
| og:image·og:url·theme-color 전 페이지 누락 | 전부 추가 + `build_og.py`로 1200x630 이미지 생성 |
| BreadcrumbList 구조화 데이터 없음 | 전 페이지 추가 (FAQPage와 별도 블록) |
| 줄자 눈금만으로 mm 선택 (모바일에서 가로 스크롤 필요) | 숫자 직접 입력칸 추가, 줄자와 양방향 동기화 |
| 모델 페이지 12줄 표에서 내 줄 찾기 어려움 | 발 길이 입력 → 해당 행 노랑 강조 + 자동 스크롤 |
| 정사이즈 모델은 '권장 mm'가 첫 열과 값이 동일 | offset 0이면 중복 열 자동 제거 |
| 표 첫 열이 이미 노란색이라 강조가 안 보임 | 첫 열을 옅은 색(#FAF8EF)으로, 강조행에 노랑 전용 |
| 한글에 자간 .1em이 붙어 글자가 흩어져 보임 | 라벨류 자간 .04em으로 완화 |
| 모바일 환산표 5칸이 3+2로 갈려 빈칸 발생 | KR/mm 전체폭 + 나머지 2x2 |
| 키보드 사용자가 눈금 20개를 통과해야 본문 도달 | 건너뛰기 링크 추가 |

### 작업 중 잡은 버그

- `setup-domain.sh`가 **교체 성공 시에만** 조용히 죽었다.
  `set -euo pipefail` + grep이 0건일 때 exit 1을 내는 조합이 원인.
  정상 경로에서만 터져서 테스트 안 했으면 그대로 배포될 뻔했다.
- og:image 눈금이 530mm까지 찍혔다 (존재하지 않는 신발 사이즈).
  사이즈 사이트에서 특히 치명적이라 230~300mm로 수정.
- 모델 페이지 JS를 f-string 템플릿에 직접 넣어 중괄호가 치환 필드로 해석됐다.
  `FINDER_JS` 상수로 분리해 해결.

---

## 9. 새 대화에서 이어갈 때

이 파일과 함께 `CLAUDE.md`를 같이 주면 좋다.
`CLAUDE.md`에는 Claude Code용 프로젝트 규칙 7개가 들어 있고,
그중 4·5번(데이터 검증 / 모델 단위 원칙)이 이 프로젝트의 핵심이다.

프로젝트 폴더 전체를 업로드하는 게 가장 확실하다.
